# Configurações do Google API
GOOGLE_APPLICATION_CREDENTIALS=credentials.json
TOKEN_FILE=token.pickle

# Escopos das APIs (não alterar a menos que saiba o que está fazendo)
# SCOPES=openid,...

# Auto-scan de documentos (opcional)
# Configure com os IDs do seu Google Drive compartilhado
AUTO_SCAN_DRIVE_ID=
AUTO_SCAN_FOLDER_ID=
